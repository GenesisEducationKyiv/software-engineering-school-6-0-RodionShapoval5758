apiVersion: v1
kind: Service
metadata:
  name: subscription
  labels:
    app.kubernetes.io/name: subscription
    app.kubernetes.io/part-of: github-release-notification
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: subscription
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: grpc
      port: 50051
      targetPort: grpc
