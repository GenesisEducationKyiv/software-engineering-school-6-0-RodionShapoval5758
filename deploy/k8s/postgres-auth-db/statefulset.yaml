apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-auth-db
  labels:
    app.kubernetes.io/name: postgres-auth-db
    app.kubernetes.io/part-of: github-release-notification
spec:
  serviceName: postgres-auth-db
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: postgres-auth-db
  template:
    metadata:
      labels:
        app.kubernetes.io/name: postgres-auth-db
        app.kubernetes.io/part-of: github-release-notification
    spec:
      containers:
        - name: postgres-auth-db
          image: postgres:16.9
          imagePullPolicy: IfNotPresent
          ports:
            - name: postgres
              containerPort: 5432
          envFrom:
            - configMapRef:
                name: postgres-auth-db-config
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-auth-db-secret
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres", "-d", "auth"]
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 10
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres", "-d", "auth"]
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
            initialDelaySeconds: 15
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              memory: 512Mi
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 5Gi
