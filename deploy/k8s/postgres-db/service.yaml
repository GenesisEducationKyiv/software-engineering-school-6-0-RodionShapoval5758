apiVersion: v1
kind: Service
metadata:
  name: postgres-db
  labels:
    app.kubernetes.io/name: postgres-db
    app.kubernetes.io/part-of: github-release-notification
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: postgres-db
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
