global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: github-release-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - subscription:8080
